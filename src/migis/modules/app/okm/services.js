import xlsx from 'xlsx';

class OKMServices {
  constructor() {}

  /** Question Section */
  handleFileUpload(file) {
    try {
      return file
    } catch (error) {
      throw error
    }
  };
  validatorFileUpload(file) {};
}

export default new OKMServices()